# Dynamic Glass & Material Specs

1. **Glassmorphism Spec**:
   - Background: `rgba(255, 255, 255, 0.04)` to `0.08`.
   - Backdrop filter: `backdrop-blur-md` (12px to 16px).
   - Border: `1px solid rgba(255, 255, 255, 0.12)`.
   - Subtle top specular highlight: `inset 0 1px 0 rgba(255, 255, 255, 0.2)`.
